# Examples

This folder contains representative source photographs and generated postcard outputs.

Current examples:

- Milano — portrait
- Lugano — landscape
- Torino — landscape

Each example follows:

```text
Original → Front → Back
```

The source images stored in `before/` have been re-saved to remove EXIF metadata before public distribution.
